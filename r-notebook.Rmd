---
title: "R Notebook"
output: html_notebook
---



```{r}
m <- matrix(rpois(50, 5), nrow = 5, ncol = 10)
colnames(m) <- c("cond1", "cond2", "cond3", "cond4", "cond5", "ctrl1", "ctrl2", "ctrl3", "ctrl4", "ctrl5")
rownames(m) <- c("gene1", "gene2", "gene3", "gene4", "geme5")

# This is the first approach I tried but gives out an error and I am not sure how to fix that
# Therefore I will treat each condition as a replicate and move forward with that (method2)

# condition <- factor(c("cond1", "cond2", "cond3", "cond4", "cond5", "ctrl1", "ctrl2", "ctrl3", "ctrl4", "ctrl5"))
# deseq_object <- DESeqDataSetFromMatrix(m, DataFrame(condition), ~condition)
# results <- DESeq(deseq_object)

# Method 2
cond <- factor(rep(1:2, each=5))
dds <- DESeqDataSetFromMatrix(m, DataFrame(cond), ~ cond)
dds <- DESeq(dds)
res <- results(dds)
```

